/* =========================================================
   Configuração da API
   Troque API_BASE_URL quando for publicar o site (ex: Render).
   ========================================================= */

const API_BASE_URL = "http://127.0.0.1:8000";
